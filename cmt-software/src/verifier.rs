use super::arith_circuit;
use rand::prelude::*;

pub struct LayerVerifier<'a> {
	circuit: &'a arith_circuit::ArithCircuit,
}

impl LayerVerifier<'_> {
	/*pub fn new(circ: &arith_circuit::ArithCircuit) -> LayerVerifier {

	}
*/}
